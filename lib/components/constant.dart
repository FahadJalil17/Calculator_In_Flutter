import 'package:flutter/material.dart';

// these are text colors
const Color greyColor = Colors.grey;

const Color whiteColor = Colors.white;

const Color blueColor = Colors.white;

// if usage of anything is multiple times then initialize it as constant and use it multiple times where required
const headingTextStyle = TextStyle(fontSize: 30, color: blueColor, fontWeight: FontWeight.w500);